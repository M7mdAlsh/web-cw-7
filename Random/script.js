// project أنشئ كائن
const project = {
    type: `man`,
    users: 200,
    biography() {    
        return `test test test`
    }
    
}


    // randomProperty أنشئ دالة

    function randomProperty(o) {
        const keys = object.keys(o);
    
        const random = Math.floor(Math.random() * keys.length)
    
    }
    
    randomProperty(project)
   